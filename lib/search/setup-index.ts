// Search index setup
// This file can be removed or kept minimal if not using Meilisearch

export const setupSearchIndex = async () => {
  // Search index setup disabled for now
  console.log('Search index setup skipped');
  return true;
};

export default setupSearchIndex;
